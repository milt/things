- content_for :controller_nav do
  = render 'layouts/controller_nav'
- content_for :content do
  = content_for?(:things_content) ? yield(:things_content) : yield
= render :template => 'layouts/application'