%table.table
  %tr
    %th ID
    %th Name
    %th Description
    %th


  - @things.each do |thing|
    %tr.thing-selector-row{id: "#{thing.id}"}
      %td= thing.id.to_s
      %td= thing.name
      %td= thing.description
      %td
#paginator
  = paginate @things, :remote => true